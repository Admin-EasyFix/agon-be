services:
  postgres_test:
    image: postgres:14-alpine
    container_name: agon_postgres_test
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: agon_test
    ports:
      - '5433:5432'
    volumes:
      - ./test-db-data:/var/lib/postgresql/data