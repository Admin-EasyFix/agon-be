services:
  db:
    image: postgres:15
    container_name: argon-db
    restart: always
    environment:
      POSTGRES_USER: "argon"
      POSTGRES_PASSWORD: "argon"
      POSTGRES_DB: "argon"
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: